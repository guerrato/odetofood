@page
@model ListModel
@{
    ViewData["title"] = "Restaurants";
}

<h1>Restaurants</h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" autofocus />
            <span class="input-group-btn">
                <button class="form-control btn btn-primary">
                    <i class="fas fa-search"></i>
                </button>
            </span>
        </div>
    </div>
</form>

@*This table can be used if you with to show the restaurants rather than panels*@
@*<table class="table">
        @foreach(var r in Model.Restaurants)
        {
            <tr>
                <td>@r.Name</td>
                <td>@r.Location</td>
                <td>@r.Cuisine</td>
                <td>
                    <a asp-page="./Detail" asp-route-restaurantId="@r.Id"><i class="fas fa-search-plus"></i></a>
                    <a asp-page="./Edit" asp-route-restaurantId="@r.Id"><i class="fas fa-edit"></i></a>
                    <a asp-page="./Delete" asp-route-restaurantId="@r.Id"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
        }
    </table>*@

@*using cards  to show the restaurants rather than panels*@
<div class="row">

    @foreach (var r in Model.Restaurants)
    {
        <partial name="_Summary" model="r" />
    }

</div> <br />
<div class="row">
    <div class="col-sm-12">
        <a asp-page="./Edit" class="btn btn-primary">Add New</a>
    </div>
</div>
@section footer {
    <p>@Model.Message</p>
}
